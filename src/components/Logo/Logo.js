import React from 'react';
import './Logo.css';

const Logo = (props) => (
  <span className="Logo">
    <span className="my">my</span><span className="xx">xx</span>
  </span>
);

export default Logo;
